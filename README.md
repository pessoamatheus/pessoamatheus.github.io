# Matheus Pessôa — Academic Research Website

GitHub Pages source for <https://pessoamatheus.github.io>.

The site is built automatically from the `main` branch by the workflow in
`.github/workflows/pages.yml`.

## Local development

```bash
npm ci
npm run dev
```

## Production build

```bash
npm run build
```

The static website is generated in `out/`.
